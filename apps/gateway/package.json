{
  "name": "@acp/gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Single entry point: authN, attribution stamping, kill-switch enforcement, task submission",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@acp/protocol": "workspace:*",
    "@acp/service-kit": "workspace:*",
    "@opentelemetry/api": "^1.9.0",
    "@temporalio/client": "^1.13.0",
    "fastify": "^5.4.0",
    "nats": "^2.29.3"
  },
  "devDependencies": {
    "@opentelemetry/sdk-trace-node": "^2.9.0",
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.4",
    "jose": "^6.0.11",
    "vitest": "^3.2.4"
  }
}
