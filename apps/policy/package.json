{
  "name": "@acp/policy",
  "version": "0.1.0",
  "private": true,
  "description": "Policy Service: Cedar PDP, default deny, git-versioned policy bundles",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@acp/protocol": "workspace:*",
    "@acp/service-kit": "workspace:*",
    "@cedar-policy/cedar-wasm": "^4.11.2",
    "fastify": "^5.4.0",
    "nats": "^2.29.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.4",
    "jose": "^6.0.11",
    "vitest": "^3.2.4"
  }
}
