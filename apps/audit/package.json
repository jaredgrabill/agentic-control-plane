{
  "name": "@acp/audit",
  "version": "0.1.0",
  "private": true,
  "description": "Audit Service: JetStream audit stream consumer into append-only Postgres; provenance queries",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@acp/protocol": "workspace:*",
    "@acp/service-kit": "workspace:*",
    "fastify": "^5.4.0",
    "nats": "^2.29.3",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/pg": "^8.15.4",
    "@vitest/coverage-v8": "^3.2.4",
    "jose": "^6.0.11",
    "vitest": "^3.2.4"
  }
}
