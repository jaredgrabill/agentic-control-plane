{
  "name": "@acp/tool-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Tool Gateway: MCP-terminating proxy that authenticates delegated tokens, decides via Cedar, rate limits, brokers upstream credentials, and audits every tool call",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@acp/protocol": "workspace:*",
    "@acp/service-kit": "workspace:*",
    "@acp/tool-client": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@opentelemetry/api": "^1.9.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@acp/mock-tools": "workspace:*",
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.4",
    "jose": "^6.0.11",
    "vitest": "^3.2.4"
  }
}
