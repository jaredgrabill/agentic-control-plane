{
  "name": "@acp/token-service",
  "version": "0.1.0",
  "private": true,
  "description": "Platform identity: short-lived JWT issuance, RFC 8693 token exchange with act-claim delegation chains, JWKS",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@acp/protocol": "workspace:*",
    "@acp/service-kit": "workspace:*",
    "@fastify/formbody": "^8.0.2",
    "fastify": "^5.4.0",
    "jose": "^6.0.11",
    "nats": "^2.29.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
