# Rubrics

The netsec golden and red-team suites are fully deterministic, so the gates
are exact: pass rate, citation precision, abstention accuracy. Judge rubrics
arrive if and when synthesis (model calls) does.
