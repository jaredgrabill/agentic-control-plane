{
  "name": "@acp/code-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Source Code agent v0: dependency queries and CI health over the mock code-forge MCP server",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@acp/agent-sdk": "workspace:*",
    "@acp/tool-client": "workspace:*"
  },
  "devDependencies": {
    "@acp/mock-tools": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@temporalio/common": "^1.13.0",
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
