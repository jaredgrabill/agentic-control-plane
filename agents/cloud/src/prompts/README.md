# Prompts

v0 is deterministic and extractive — answers are templated from tool data,
no model calls, so there are no prompts yet. When synthesis arrives, prompts
live here under version control (agent-patterns.md).
