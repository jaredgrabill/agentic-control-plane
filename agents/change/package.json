{
  "name": "@acp/change-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Change/ITSM agent v0: conflict checks and governed change writes (draft, submit, withdraw) over the mock itsm MCP server",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@acp/agent-sdk": "workspace:*",
    "@acp/tool-client": "workspace:*"
  },
  "devDependencies": {
    "@acp/mock-tools": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@temporalio/common": "^1.13.0",
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
