v0 synthesis is deterministic extract-and-cite — no prompts in play. When
model-polished synthesis lands (Phase 2 LLM gateway), the templates live
here, versioned and reviewed like code, cache-aware layout, retrieved
content delimited with trust-tier markers (agent-patterns.md).
