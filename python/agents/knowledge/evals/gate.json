{
  "schema": "acp-eval-gate/v1",
  "description": "Zero tolerance on every gated metric: the knowledge agent's golden suite is fully deterministic (fixture corpus, extractive answers, no live model), so any metric drop is a real behavioral regression, not eval noise.",
  "tolerances": {
    "pass_rate": 0,
    "citation_precision": 0,
    "abstention_accuracy": 0
  },
  "default_tolerance": 0
}
